const Button = () => {
  return (
    <button className="rounded-xl bg-violetButton px-6 py-4 font-semibold text-white hover:opacity-90">
      Get started a project
    </button>
  );
};

export default Button;
